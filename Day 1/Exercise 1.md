Exercises: What is React? = React is a JS library for building user interfaces.
What is React? = Because it makes the process of building modular, reusable user interface components easier and more efficient in a single page application.
What is a library? = A library is a collection of pre-written code that developers can use to optimize tasks.
What is a framework? = A framework is a pre-defined structure that provides a foundation for building applications.
What is a single page application? = A single page application is a web application that loads a single HTML page and dynamically updates the content as the user interacts with the app.
What is a component ? = A component is a reusable piece of code that defines how a part of the UI should look and behave.
What is the latest version of React? = The latest version of React is 18.2.0.
What is DOM? = The DOM (Document Object Model) is a programming interface for HTML and XML documents, representing the structure of a webpage.
What is React Virtual DOM? = The React Virtual DOM is an in-memory representation of the real DOM, used to optimize rendering performance by minimizing direct manipulation of the actual DOM.
What does a web application or a website(composed of) have? = A web application or a website is composed of HTML, CSS, and JavaScript.

Exercises: Why React? = Because it makes the process of building modular, reusable user interface components easier and more efficient in a single page application.  
Why did you chose to use react? = Because it allows for building dynamic and interactive user interfaces with reusable components, improving development efficiency and user experience.
What are the advantages of using React? = Some advantages of using React include component-based architecture, virtual DOM for performance, unidirectional data flow, and a large ecosystem of libraries and tools.
What are the disadvantages of using React? = Some disadvantages of using React include a steep learning curve for beginners, frequent updates that may require code changes, and the need for additional libraries for state management and routing.
What measures do you use to know popularity ? = Measures to determine popularity include GitHub stars, npm downloads, Stack Overflow questions, and job market demand.
What is more popular, React or Vue ? = React is more popular than Vue.

Exercises: JSX
What is an HTML element? = An HTML element is a building block of a webpage, defined by tags that structure and format content.
How to write a self closing HTML element? = A self-closing HTML element is written with a slash before the closing angle bracket, like this: <br /> or <img src="image.jpg" />.
What is an HTML attribute? Write some of them = An HTML attribute provides additional information about an element, such as class, id, src, alt, etc.
What is JSX? = JSX is a syntax extension for JavaScript that allows writing HTML-like code in JavaScript files.
What is babel? = Babel is a JavaScript compiler that converts JSX into regular JavaScript.
What is a transpiler? = A transpiler (or "source-to-source compiler") transforms source code from one programming language to another.
Exercises: JSX Elements = There are two types of JSX elements: self-closing elements and container elements.
What is a JSX element? = A JSX element is a syntax that looks similar to HTML and is used in React to describe the UI structure.
Write your name in a JSX element and store it in a name variable = const name = <h1>Junaid Abbasi</h1>;
Write a JSX element which displays your full name, country, title, gender, email, phone number. Use h1 for the name and p for the rest of the information and store it in a user variable
Write a footer JSX element

Exercises: Inline Style
Create a style object for the main JSX
Create a style object for the footer and app JSX
Add more styles to the JSX elements

Exercises: Internal Styles
Apply different styles to your JSX elements

Exercise: Inject data to JSX
Practice how to make JSX element and injecting dynamic data(string, number, boolean, array, object)
