import React from "react";

//here we made our context and also we have to make provider and then wrap into div to use as a context 
const UserContext = React.createContext()




export default UserContext

